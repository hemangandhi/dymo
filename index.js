const xml = require('xml2json');

// let window = {setTimeout : setTimeout, JSON : JSON};
const api = require('./test.js');

console.log(global);

let printers = dymo.label.framework.getPrinters();
